package com.food.core.base.activity

import androidx.appcompat.app.AppCompatActivity

/**
 * Created by mikekojansow on 24/07/20.
 * Senior Android Developer
 */
open class BaseActivity(private val layoutResourceId: Int): AppCompatActivity(layoutResourceId) {
}